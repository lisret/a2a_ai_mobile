<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ========== 核心功能权限 ========== -->
    <!-- 网络访问权限：用于连接AI模型API服务（必需） -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ========== 自动化功能权限 ========== -->
    <!-- 无障碍服务权限：用于自动化操作（截图、点击、滑动等）（必需） -->
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
    <!-- Android 8.0+ 前台服务权限：用于在后台执行自动化任务（必需） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14+ 特殊用途前台服务权限：用于自动化任务执行（必需） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- 唤醒锁权限：防止系统休眠，确保任务正常执行（必需） -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Android 11+ 查询所有应用权限：用于启动和管理其他应用（必需） -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- ========== 通知功能权限 ========== -->
    <!-- Android 13+ 通知权限：用于显示任务完成通知和任务执行状态（可选，建议授予） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ========== 辅助功能权限 ========== -->
    <!-- 相机权限：用于扫描二维码添加API密钥（可选） -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <!-- 读取存储权限：用于读取图片等文件（可选） -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- 写入存储权限：用于保存截图等文件（可选） -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 悬浮窗权限：用于显示任务执行状态悬浮窗（可选） -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
      android:name=".core.MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme">
      <activity
        android:name=".core.MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
      
      <!-- AutoGLM 无障碍服务 -->
      <service
        android:name=".service.AutoGLMAccessibilityService"
        android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
        android:exported="false">
        <intent-filter>
          <action android:name="android.accessibilityservice.AccessibilityService" />
        </intent-filter>
        <meta-data
          android:name="android.accessibilityservice"
          android:resource="@xml/accessibility_service_config" />
      </service>
      
      <!-- 任务执行前台服务 -->
      <service
        android:name=".service.TaskExecutionService"
        android:enabled="true"
        android:exported="false"
        android:foregroundServiceType="specialUse" />
      
      <!-- Headless JS 任务执行服务 -->
      <service
        android:name=".service.TaskExecutionHeadlessService"
        android:enabled="true"
        android:exported="false"
        android:foregroundServiceType="specialUse" />
    </application>
</manifest>
